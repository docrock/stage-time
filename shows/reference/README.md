# Reference rundowns

Real shows, kept here as the source material for the Stage Time data model. If a rundown
in here can't be represented cleanly in a `shows/*.json` file, the model is wrong.

Contains real team and vendor contact details. This repo is private — keep it that way.
